// ids

const ROOT_ID = "root";

export { ROOT_ID };

// routes

const FLUX_ROUTE = "/flux";
const LOGIN_ROUTE = "/login";
const AUTH_ROUTE = "/";

export { AUTH_ROUTE, FLUX_ROUTE, LOGIN_ROUTE };

// animation

const FRAMES_PER_SECOND = 30;
const FRAME_DURATION = 1000 / FRAMES_PER_SECOND;

export { FRAME_DURATION, FRAMES_PER_SECOND };
